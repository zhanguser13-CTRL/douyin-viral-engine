# 🚀 一键部署指南

## 第一步：发布到 GitHub（使用 GitHub Desktop）

你已经打开了 GitHub Desktop，请按照以下步骤操作：

### 在 GitHub Desktop 中：

1. **查看更改**
   - 你应该能看到所有 33 个文件已经提交（Initial commit）

2. **发布仓库**
   - 点击顶部的 **"Publish repository"** 按钮
   - 或者点击 **"Repository"** 菜单 → **"Push"**

3. **配置仓库信息**
   - **Name**: `douyin-viral-master`（或你喜欢的名字）
   - **Description**: `抖音爆款引擎 - AI 驱动的文案生成工具`
   - **Keep this code private**: 根据需要选择（建议取消勾选，设为公开）
   - 点击 **"Publish Repository"**

4. **等待上传完成**
   - GitHub Desktop 会自动推送所有代码
   - 完成后你会看到 "Last fetched just now"

---

## 第二步：部署到 Vercel（推荐）

### 方式 A：使用浏览器（最简单）

1. **访问 Vercel**
   - 打开 https://vercel.com
   - 点击 **"Sign Up"** 或 **"Log In"**
   - 选择 **"Continue with GitHub"**

2. **导入项目**
   - 登录后点击 **"Add New..."** → **"Project"**
   - 在列表中找到 `douyin-viral-master` 仓库
   - 点击 **"Import"**

3. **配置项目**
   - Framework Preset: **Vite** (自动检测)
   - Root Directory: `./` (默认)
   - Build Command: `npm run build` (自动填充)
   - Output Directory: `dist` (自动填充)

4. **添加环境变量** ⚠️ 重要！
   - 展开 **"Environment Variables"** 部分
   - 添加：
     - **Key**: `GEMINI_API_KEY`
     - **Value**: `你的 Gemini API 密钥`
     - 获取地址：https://aistudio.google.com/app/apikey
   - 点击 **"Add"**

5. **部署**
   - 点击 **"Deploy"** 按钮
   - 等待 2-3 分钟
   - 完成！你会得到一个 `.vercel.app` 域名

---

## 第三步：部署到 Netlify

1. **访问 Netlify**
   - 打开 https://app.netlify.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 **"Add new site"** → **"Import an existing project"**
   - 选择 **"GitHub"**
   - 找到 `douyin-viral-master` 仓库并选择

3. **配置构建**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - 点击 **"Deploy site"**

4. **添加环境变量**
   - 部署完成后，进入 **Site settings** → **Environment variables**
   - 点击 **"Add a variable"**
   - Key: `GEMINI_API_KEY`
   - Value: `你的 Gemini API 密钥`
   - 点击 **"Save"**

5. **重新部署**
   - 进入 **Deploys** 标签
   - 点击 **"Trigger deploy"** → **"Deploy site"**

---

## 🔑 获取 Gemini API Key

1. 访问 https://aistudio.google.com/app/apikey
2. 登录 Google 账号
3. 点击 **"Create API Key"**
4. 复制生成的密钥
5. 在 Vercel 和 Netlify 的环境变量中使用

---

## ✅ 完成检查清单

- [ ] GitHub Desktop 中点击 "Publish repository"
- [ ] 代码已成功推送到 GitHub
- [ ] 在 Vercel 导入项目
- [ ] 在 Vercel 添加 GEMINI_API_KEY 环境变量
- [ ] Vercel 部署成功
- [ ] 在 Netlify 导入项目
- [ ] 在 Netlify 添加 GEMINI_API_KEY 环境变量
- [ ] Netlify 部署成功

---

## 🎉 部署完成后

你会得到两个访问地址：
- **Vercel**: `https://你的项目名.vercel.app`
- **Netlify**: `https://你的项目名.netlify.app`

两个平台都会自动：
- ✅ 全球 CDN 加速
- ✅ 自动 HTTPS
- ✅ 每次推送代码自动重新部署

---

## 💡 提示

- 如果部署失败，检查环境变量是否正确配置
- 两个平台都提供免费额度，足够个人使用
- 可以在平台设置中绑定自定义域名

现在就开始吧！🚀
